name: fnirs-flow
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - mne>=1.6
  - mne-nirs>=0.7
  - scipy
  - numpy
  - pydantic
  - pip:
    - fnirs-flow[api,mne,ml-motion]
    - ruff
    - pytest
    - pytest-cov
