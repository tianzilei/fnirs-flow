# MethodAtom Library QA Report

- Validation command: `.venv/Scripts/python.exe C:/Users/John/.codex/skills/literature-to-methodatom-library/scripts/validate_methodatom_library.py fnirs_flow/registry/methodatom_lib`
- Status: PASS
- Errors: 0
- Warnings: 0

## Object counts

- sources: 1640
- method atoms: 114
- parameter candidates: 417
- adapter definitions: 1
- flow slot contracts: 55
- risk rule candidates: 7
- reporting requirements: 8
- flow template candidates: 5
- evidence links: 3244

## Default promotion policy

No literature parameter is promoted merely because it is reported, inferred,
or common. Runtime defaults are created only for parameters explicitly marked
`project_default`. All other values remain in `parameter_candidates.csv` and
MethodAtom provenance metadata.

## Human review queue

- Repository-local citation records provide titles for all 1,640 source rows;
  1,637 rows carry PubMed identifiers and years. External validation was not
  performed, so publication use still requires normal citation verification.
- The five flow-template rows now contain complete atom, slot, risk, reporting,
  input, and output references. They remain `needs_attention` until approved by
  a scenario owner.
- Unverified backend bindings remain `needs_attention` and have no executable
  backend handler. Only contract-tested Cedalion bindings are executable.

## Repository quality gates

- Every parameter has an explicit evidence status; no missing status is
  silently promoted to an executable default.
- Port handles are schema-independent snake_case identifiers.
- Domains use the canonical MethodAtom data-model enumeration.
- Shared operations expose explicit template-specific contract variants.
- `outputs/methodatom_library` is a generated byte-for-byte mirror of this
  packaged library.
